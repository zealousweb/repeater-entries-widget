name: Deploy to WordPress.org
on:
  push:
    tags:
    - "*"
    branches:
    - master

jobs:
  tag:
    name: Deply Plugin
    runs-on: ubuntu-latest
    steps:
    - name: WordPress Plugin Deploy
      uses: zealousweb/repeater-entries-widget@master
      env:
        SVN_PASSWORD: ${{ secrets.SVN_PASSWORD }}
        SVN_USERNAME: ${{ secrets.SVN_USERNAME }}